<% content_for(:title, "대여관리 - Admin - Today Bike") %>

<div class="w-full">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-gray-900">대여관리</h1>
    <%= link_to new_admin_rental_path, class: "inline-flex items-center gap-2 rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 transition" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
      </svg>
      New Rental
    <% end %>
  </div>

  <%# Type filter tabs %>
  <div class="mb-4">
    <nav class="flex gap-2 flex-wrap" aria-label="Type filter">
      <%= link_to "All", admin_rentals_path, class: "px-3 py-1.5 text-sm font-medium rounded-md #{params[:rental_type].blank? ? 'bg-indigo-600 text-white' : 'bg-white text-gray-700 ring-1 ring-inset ring-gray-300 hover:bg-gray-50'} transition" %>
      <% Rental::RENTAL_TYPE_LABELS.each do |key, label| %>
        <%= link_to label, admin_rentals_path(rental_type: key), class: "px-3 py-1.5 text-sm font-medium rounded-md #{params[:rental_type] == key ? 'bg-indigo-600 text-white' : 'bg-white text-gray-700 ring-1 ring-inset ring-gray-300 hover:bg-gray-50'} transition" %>
      <% end %>
    </nav>
  </div>

  <div class="bg-white shadow rounded-lg overflow-hidden">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Daily Rate</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Active</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Bookings</th>
          <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% @rentals.each do |rental| %>
          <tr>
            <td class="px-6 py-4 text-sm text-gray-900">
              <%= link_to rental.name, admin_rental_path(rental), class: "text-indigo-600 hover:text-indigo-900 font-medium" %>
            </td>
            <td class="px-6 py-4 text-sm">
              <span class="inline-flex items-center rounded-full bg-blue-100 px-2.5 py-0.5 text-xs font-medium text-blue-800"><%= rental.rental_type_label %></span>
            </td>
            <td class="px-6 py-4 text-sm text-gray-900">
              <%= number_to_currency(rental.daily_rate, unit: "₩", precision: 0) %>/day
            </td>
            <td class="px-6 py-4 text-sm">
              <% if rental.active? %>
                <span class="inline-flex items-center rounded-full bg-green-100 px-2.5 py-0.5 text-xs font-medium text-green-800">Active</span>
              <% else %>
                <span class="inline-flex items-center rounded-full bg-gray-100 px-2.5 py-0.5 text-xs font-medium text-gray-800">Inactive</span>
              <% end %>
            </td>
            <td class="px-6 py-4 text-sm text-gray-900">
              <%= link_to "#{rental.rental_bookings.count} bookings", admin_rental_rental_bookings_path(rental), class: "text-indigo-600 hover:text-indigo-900" %>
            </td>
            <td class="px-6 py-4 text-right text-sm">
              <div class="flex items-center justify-end gap-2">
                <%= link_to "Edit", edit_admin_rental_path(rental), class: "text-indigo-600 hover:text-indigo-900" %>
                <%= button_to "Delete", admin_rental_path(rental), method: :delete,
                      class: "text-red-600 hover:text-red-900",
                      form: { data: { turbo_confirm: "Are you sure?" } } %>
              </div>
            </td>
          </tr>
        <% end %>

        <% if @rentals.empty? %>
          <tr>
            <td colspan="6" class="px-6 py-12 text-center text-sm text-gray-500">
              No rentals yet. <%= link_to "Create the first one", new_admin_rental_path, class: "text-indigo-600 hover:text-indigo-500 font-medium" %>.
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <% if @pagy.pages > 1 %>
      <div class="border-t border-gray-200 bg-white px-4 py-3 sm:px-6">
        <div class="flex items-center justify-between">
          <div class="text-sm text-gray-700">
            Total <span class="font-medium"><%= @pagy.count %></span> rentals
          </div>
          <nav class="isolate inline-flex -space-x-px rounded-md shadow-sm" aria-label="Pagination">
            <% if @pagy.prev %>
              <%= link_to pagy_url_for(@pagy, @pagy.prev), class: "relative inline-flex items-center rounded-l-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 transition" do %>
                <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M11.78 5.22a.75.75 0 0 1 0 1.06L8.06 10l3.72 3.72a.75.75 0 1 1-1.06 1.06l-4.25-4.25a.75.75 0 0 1 0-1.06l4.25-4.25a.75.75 0 0 1 1.06 0Z" clip-rule="evenodd" /></svg>
              <% end %>
            <% end %>
            <% @pagy.series.each do |item| %>
              <% case item %>
              <% when Integer %>
                <%= link_to item, pagy_url_for(@pagy, item), class: "relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-900 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 transition" %>
              <% when String %>
                <span aria-current="page" class="relative z-10 inline-flex items-center bg-indigo-600 px-4 py-2 text-sm font-semibold text-white"><%= item %></span>
              <% when :gap %>
                <span class="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-700 ring-1 ring-inset ring-gray-300">&hellip;</span>
              <% end %>
            <% end %>
            <% if @pagy.next %>
              <%= link_to pagy_url_for(@pagy, @pagy.next), class: "relative inline-flex items-center rounded-r-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 transition" do %>
                <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M8.22 5.22a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 0 1-1.06-1.06L11.94 10 8.22 6.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" /></svg>
              <% end %>
            <% end %>
          </nav>
        </div>
      </div>
    <% end %>
  </div>
</div>
