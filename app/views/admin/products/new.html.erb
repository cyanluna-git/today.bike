<% content_for(:title, "New Product - Admin - Today Bike") %>

<div class="w-full max-w-3xl">
  <div class="mb-6">
    <%= link_to admin_products_path, class: "inline-flex items-center gap-1 text-sm text-gray-500 hover:text-gray-700 transition" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      Back to Products
    <% end %>
  </div>

  <h1 class="text-2xl font-bold text-gray-900 mb-6">New Product</h1>

  <%= render "form", product: @product %>
</div>
